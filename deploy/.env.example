# ── Base de datos ──
DB_USERNAME=sgs_user
DB_PASSWORD=CAMBIA_ESTA_PASSWORD_SEGURA

# ── JWT ──
JWT_SECRET=REEMPLAZA_CON_BASE64_SEGURO

# ── SSL local/keystore (si aplica) ──
SSL_KEYSTORE_PASSWORD=CAMBIA_ESTA_PASSWORD_SEGURA

# ── Mail ──
MAIL_USERNAME=tu-correo@gmail.com
MAIL_PASSWORD=tu-app-password

# ── Mercado Pago ──
MP_ACCESS_TOKEN=APP_USR_REEMPLAZAR
MP_NOTIFICATION_URL=https://sgsmarket.duckdns.org/api/pagos/webhook

# ── CORS (origenes extra separados por coma) ──
CORS_ALLOWED_ORIGINS=https://sgsmarket.duckdns.org

# ── Admin bootstrap ──
ADMIN_EMAIL=admin@tu-dominio.com
ADMIN_PASSWORD=CAMBIA_ESTA_PASSWORD_ADMIN
