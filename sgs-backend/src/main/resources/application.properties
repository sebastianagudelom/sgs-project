spring.application.name=sgs-backend

# ── MySQL ──
spring.datasource.url=jdbc:mysql://localhost:3306/sgs_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=sebas031800

# ── JPA / Hibernate ──
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ── Server ──
server.port=8080

# ── HTTPS / SSL ──
server.ssl.enabled=false
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=sgssupermercado
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=sgs

# ── File Upload ──
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
app.upload-dir=uploads

# ── JWT ──
jwt.secret=U0dTU3VwZXJtYXJrZXRTZWNyZXRLZXlGb3JKV1RUb2tlbkdlbmVyYXRpb24yMDI2IQ==
jwt.expiration=86400000

# ── Mail (Gmail SMTP) ──
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sebastianagudelomendez@gmail.com
spring.mail.password=hwhe elnb itrf gevb
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ── Mercado Pago ──
mercadopago.access-token=APP_USR-5847473122030590-041710-0a2adc8451403625d2d6a191a4690ce8-3343500104
mercadopago.notification-url=https://equinox-undermine-ditch.ngrok-free.dev/api/pagos/webhook

# ── App ──
app.frontend-url=http://localhost:4200

# ── Actuator / Prometheus ──
management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.endpoints.access.default=read-only
management.endpoint.health.show-details=when-authorized
management.metrics.tags.application=${spring.application.name}
management.metrics.tags.environment=dev
# Habilita buckets de histograma para latencia (p50/p95/p99)
management.metrics.distribution.percentiles-histogram.http.server.requests=true
management.metrics.distribution.slo.http.server.requests=50ms,100ms,200ms,500ms,1s,2s